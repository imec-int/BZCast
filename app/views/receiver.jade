doctype html5
html
	head
		meta(name='viewport', content='initial-scale=1.0, user-scalable=no')
		title= title
		link(href='http://fonts.googleapis.com/css?family=Roboto:400,700,400italic,100', rel='stylesheet', type='text/css')

		link(rel='stylesheet', href="/stylesheets/receiver.css")

		script(type='text/javascript', src='//code.jquery.com/jquery.min.js')
		script(type='text/javascript', src='/vendor/jquery.tmpl.min.js')
		script(src='/socket.io/socket.io.js')
		script.
			var Config = !{JSON.stringify(Config)}; // appid and namespace coming from the server
		script(type='text/javascript', src='/javascripts/receiver.js')

		script#player-tmpl(type="text/x-jquery-tmpl")
			.player(class="${dead?'dead':''}")
				.name ${name}
				.flagcode ${flagcode}
				.flagname ${flagname}
				.score ${score}
				.killsanddeads ${kills}-${deaths}

	body
		.container
			#players
			audio